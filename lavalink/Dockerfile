# Lavalink — config'i imaja göm (Coolify bind-mount'a güvenmek yerine).
# Pluginler (youtube-source + LavaSrc) application.yml'deki listeden açılışta indirilir.
FROM ghcr.io/lavalink-devs/lavalink:4
COPY application.yml /opt/Lavalink/application.yml
